import styled from 'styled-components';

const AppWrapper = styled.div`
  box-sizing: border-box;
  background: url(sky_and_clouds.jpg), rgba(173, 211, 245, 0.5);
  background-size: cover;
`;

export default AppWrapper;
