import styled from 'styled-components';

const AppWrapper = styled.div`
  box-sizing: border-box;
  background-size: cover;
  background-color: rgba(173, 211, 245, 0.5);
`;

export default AppWrapper;
